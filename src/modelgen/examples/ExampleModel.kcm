#model

enum ExampleEnum {
    ValueA, ValueB, ValueC, ValueD, ValueE,
}

enum State {
    idle, connected, working,
}

model Account {
    name: str,
    passwd: str,
}

model ExampleModel {
    account: Account,
    fieldB: bool,
}
